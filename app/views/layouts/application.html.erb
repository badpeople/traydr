<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
        "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html>
<head>
  <title>Traydr.com: <%= h(yield(:title) || "Untitled") %></title>
  <%= stylesheet_link_tag 'application' %>
  <%= yield(:head) %>
</head>
<body>
<div id="container">
  <div id="header">
    <span id="header_sitename">
      Traydr.com
    </span>

    <span id="account_controls">
      <% if logged_in? %>
          Welcome <%= h User.find(session[:user_id]).username %>! Not you?
          <%= link_to "Log out", logout_path %>
      <% else %>
          <%= link_to "Sign up", signup_path %> or
          <%= link_to "log in", login_path %>.
      <% end %>
    </span>
  </div>
  <div id="navigation">
    <ul>
      <% if logged_in? then %>
          <li><%= link_to 'Dashboard', :controller=>'users', :action=>'home'%></li>
      <% else %>
          <li><%= link_to 'Dashboard', :controller=>'signup' %></li>
      <% end %>

      <li><%= link_to 'Systems', :controller=>'systems' %></li>
      <li><%= link_to 'Alerts', :controller=>'dashboard' ,:action=>'alerts'%></li>
      <li><a href="#">Contact us</a></li>
    </ul>
  </div>
  <div id="content">
    <%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
    <%- end -%>

    <%- if show_title? -%>
        <h1><%= h yield(:title) %></h1>
    <%- end -%>

    <%= yield %>
  </div>
</div>

  <div id="footer">
    Copyright � traydr.com, 2010
  </div>
</body>
</html>
